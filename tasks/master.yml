---
- name: k3s master | get token
  slurp:
    src: /var/lib/rancher/k3s/server/node-token
  register: k3s_token

- name: k3s master | save token
  set_fact:
    k3s_token: "{{ k3s_token.content }}"
