---
k3s_mode: agent
k3s_group: k3s
k3s_url: 
k3s_config:
k3s_labels: []
k3s_interface: "{{ ansible_default_ipv4.interface }}"
k3s_iptables: []

# curl -w '%{redirect_url}' -so /dev/null | sed 's|.*/||'
#k3s_version: "{{ lookup('url', k3s_latest_url) | regex_replace('^.*\/tag\/') }}"
k3s_version: v1.19.3+k3s3
