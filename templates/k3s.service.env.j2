# environment variables for k3s systemd service
# {{ ansible_managed }}

K3S_URL={{ k3s_url }}
K3S_TOKEN_FILE=/var/lib/rancher/k3s/server/node-token

{% for key, val in k3s_env %}
{{ key }}={{ val }}
{% endfor %}
