---
# {{ ansible_managed }}

node-name: {{ inventory_hostname_short }}

{% if k3s_mode != 'server' %}
server: "{{ k3s_url }}"
{% if k3s_token | d() %}
token: "{{ k3s_token }}"
{% endif %}
{% endif %}

{{ k3s_config | to_nice_yaml }}
